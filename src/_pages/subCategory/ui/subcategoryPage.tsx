import { SubCategoryPageProps } from '../model/types';

export const SubcategoryPage = ({ path }: SubCategoryPageProps) => {
  return <div>Здесь будет динамически сформированаая подкатегория {path}</div>;
};
